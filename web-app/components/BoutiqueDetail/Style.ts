import styled from "styled-components";

export const Logo = styled.div`
  position: relative;
  height: 20rem;
`;

export const Wrapper = styled.div`
  flex: 1;
  padding: var(--padding);
  align-self: center;
  max-width: 80rem;
  width: 100%;

`;
